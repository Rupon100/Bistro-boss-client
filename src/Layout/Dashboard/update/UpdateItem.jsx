import SectionTitle from "../../../components/SectionTitle/SectionTitle";

 

const UpdateItem = () => {
    return (
        <div>
            <SectionTitle subHeading={`Refresh Info`} heading={`Update Menu Item`} ></SectionTitle>
            not implement till now
        </div>
    );
};

export default UpdateItem;